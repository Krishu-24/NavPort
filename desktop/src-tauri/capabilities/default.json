{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum the NavPort shell needs. Tauri v2 denies every command that isn't granted here, so anything the webview could otherwise reach — the filesystem, shell execution, arbitrary HTTP from Rust — stays unreachable because it is simply never granted.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
